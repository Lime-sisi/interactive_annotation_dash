name: Keep sample_driven_dash app Awake

on:
  schedule:
    - cron: "*/5 * * * *"  # every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render dash app
        run: curl -s https://interactive-annotation-dash.onrender.com > /dev/null